mod annotated;
mod list;
mod map;
mod mode;
mod problem;
mod problems;
mod variant;

/// Tags for [Depict](::depiction::Depict).
pub mod tag;

#[allow(unused_imports)]
pub use {annotated::*, list::*, map::*, mode::*, problem::*, problems::*, variant::*};
